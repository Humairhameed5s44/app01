export default function Student(props){
    return(
        <div>
            name={props.name}
            population={props.population}
            capital={props.capital}
        </div>
    )
}